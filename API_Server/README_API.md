# API Server for Smart Farm System

This Flask API receives sensor data from the ESP32 and uses the trained machine learning
model to predict crop disease and return control decisions.

## Run the API:

